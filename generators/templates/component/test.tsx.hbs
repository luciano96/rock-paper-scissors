import {{ pascalCase componentName }} from '../{{ pascalCase componentName }}';

describe('<{{ pascalCase componentName }} />', () => {
  it('should render correctly', () => {
    cy.mount(<{{ pascalCase componentName }}>Click me!</{{ pascalCase componentName }}>)
    cy.get('div').should('contains.text', 'Hello {{ pascalCase componentName }}')
  });
});
